# Git's GIT_ASKPASS interface requires a whole program name, it can't handle arguments.
# This takes all the needed arguments as environment variables and constructs the real call
"$GOT_PYTHON" "$GOT_SCRIPT" --get-credential "$GOT_HOSTNAME"
